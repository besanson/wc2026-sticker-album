# scenarios

User-authored what-if scenario files for `matchsim whatif --scenario <file>.yaml`.
Each file maps player IDs to explicit, user-typed flags — `available`,
`minutes_cap`, `fitness_multiplier` — that the model applies as-is. These
values are never inferred from data (CLAUDE.md section 1).
